<resources>

  <!-- Base application theme. -->
  <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
    <!-- Customize your theme here. -->
    <!-- Main theme colors -->
    <!--   your app branding color for the app bar, also for button color-->
    <item name="android:colorPrimary">@color/primary</item>
    <!--   darker variant for the status bar and contextual app bars -->
    <item name="android:colorPrimaryDark">@color/primary_dark</item>
    <!--   theme UI controls like checkboxes and text fields -->
    <item name="android:colorAccent">@color/accent</item>
    <item name="android:datePickerDialogTheme">@style/MyDialog</item>
    <item name="android:timePickerDialogTheme">@style/MyDialog</item>
    <!--progress bar theme-->
    <item name="android:progressBarStyle">@style/MyProgress</item>
    <!--switch button theme-->
    <item name="android:switchStyle">@style/MySwitch</item>
  </style>

  <style name="MyDialog" parent="Theme.AppCompat.Light.Dialog">
    <item name="colorAccent">@color/accent</item>
    <!-- Used for the title and text -->
    <item name="android:textColorPrimary">@color/primary_text</item>
    <!-- Used for the background -->
    <!--<item name="android:background">@color/primary_background</item>-->
  </style>

  <style name="MyProgress" parent="Widget.AppCompat.ProgressBar.Horizontal">
    <item name="android:background">@color/primary_light</item>
  </style>

  <style name="MySwitch" parent="Widget.AppCompat.CompoundButton.Switch"></style>

</resources>
